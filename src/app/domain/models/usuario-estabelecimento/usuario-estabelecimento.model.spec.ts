import { UsuarioEstabelecimentoModel } from './usuario-estabelecimento.model';

describe('UsuarioEstabelecimentoModel', () => {
  it('should create an instance', () => {
    expect(new UsuarioEstabelecimentoModel()).toBeTruthy();
  });
});
